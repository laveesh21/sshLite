# sshLite
A lightweight web-based SSH client built with Node.js, enabling secure remote server access directly from the browser. Features real-time terminal interaction, authentication, and a minimal, responsive interface.
